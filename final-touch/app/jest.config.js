/* This JavaScript code snippet is configuring Jest for a Next.js project. Here's a breakdown of what
each part is doing: */
// const nextJest = require('next/jest');
// const createJestConfig = nextJest ? .default({ dir: './' }) || nextJest({ dir: './' });
// const customJestConfig = {
//     // Other config options...
//     moduleNameMapper: {
//         // Resolve react module with the Next.js internal one
//         react: 'next/dist/compiled/react/cjs/react.development.js',
//         '^@/(.*)$': '<rootDir>/src/$1',
//     },
// };
// const configs = createJestConfig(customJestConfig);
// module.exports = configs;